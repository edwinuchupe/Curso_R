# Nombre: Edwin Uchupe Pipa 20151478A

# PROBLEMA 4

# a)

f1 <- array(data = 1:36, dim = c(3,3,4))#Es un arreglo de tres dimensiones y con la funcion array esta
# dada explicitamente.
bar <- as.vector(f1)#Este es un vector y por la funcion as.vector se da explicitamente.
baz <- as.character(bar)# Este es un vector de caracteres y esta dada implicitamente.
qux <- as.factor(baz)#Este es un factor esta dada explicitamente.
quux <- bar + c(-0.1, 0.1)# Este tambien es un vector y esta dada implicitamente. Dado bar es vector numerico 
# y suma de vectores numericos da un vector numerico.


